# Environment variables for the application

# The environment the application is running in (development or production)
NODE_ENV=development

# The port the server will listen on
PORT=8000

# The full URL to connect to the database
# Example: mysql://user:password@host:port/database
DATABASE_URL="mysql://root:password@localhost:3306/hono"

# A strong, random secret for signing session tokens (at least 32 characters)
SESSION_SECRET="a-very-strong-and-super-secret-key-of-at-least-32-chars"
